package com.revature.models;

import javax.persistence.Entity;
import javax.persistence.Table;

@Entity
@Table(name="users")
public class Users 
{
	@Id
	@GeneratedValue(Strategy = GenerationType.IDENTITY)
	@Column(name="user_id")//set the yser id column name
	
}
